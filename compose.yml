services:
  bot:
    build:
      context: .
    env_file:
      - .env
    ports:
      - '3000:3000'
    init: true
    restart: unless-stopped
    cpus: 0.50
    mem_limit: 256m
    security_opt:
      - no-new-privileges:true
