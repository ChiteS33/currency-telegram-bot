# C4 Model Diagrams Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Document the Currency Telegram Bot using maintainable C4 Model PlantUML source diagrams.

**Architecture:** Keep C4 sources in `docs/c4/` and link them from the project README. The diagrams reflect the current long-polling Telegram flow, Fastify health endpoint, clean application boundaries, and Frankfurter HTTP integration; they do not change runtime code.

**Tech Stack:** PlantUML with the built-in C4-PlantUML standard library; Markdown.

**Spec:** `docs/superpowers/specs/2026-09-07-currency-telegram-bot-design.md`

## Global Constraints

- Store editable PlantUML source in `docs/c4/`.
- Include Context, Container, and Component C4 levels.
- Do not commit rendered SVG or PNG artifacts.
- Describe the implemented architecture only; do not imply databases, webhooks, caching, or additional HTTP rate endpoints.

---

### Task 1: Add C4 PlantUML source diagrams

**Files:**
- Create: `docs/c4/01-system-context.puml`
- Create: `docs/c4/02-container.puml`
- Create: `docs/c4/03-component.puml`

**Interfaces:**
- Produces: three independently renderable PlantUML files using `!include <C4/...>`.

- [x] Create the context diagram with the Telegram user, Telegram platform, Currency Telegram Bot, and Frankfurter API.
- [x] Create the container diagram with one Node.js process containing Telegraf long polling, Fastify `GET /health`, and application logic.
- [x] Create the component diagram with the Telegram message handler, `GetExchangeRate`, `ExchangeRateProvider`, and `FrankfurterExchangeRateProvider`.
- [x] Verify each relation represents the current source code flow and labels its protocol or purpose.

### Task 2: Document rendering and discovery

**Files:**
- Create: `docs/c4/README.md`
- Modify: `README.md`
- Modify: `docs/superpowers/specs/2026-09-07-currency-telegram-bot-design.md`

**Interfaces:**
- Produces: a documented renderer command and repository links to the diagram sources.

- [x] Explain the three diagram levels and their scope in `docs/c4/README.md`.
- [x] Document a PlantUML SVG rendering command without adding a project dependency.
- [x] Link the C4 documentation from the project README.
- [x] Record the diagram policy in the architecture spec.

## Plan self-review

- Spec coverage: the two tasks cover all three required C4 levels, their source location, rendering guidance, and README discoverability.
- No placeholders: all files and diagram subjects are named explicitly.
- Type consistency: diagram names mirror the existing TypeScript classes and modules.
